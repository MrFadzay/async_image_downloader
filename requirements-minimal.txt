# Минимальные зависимости (без браузерного режима)
# Минимальная версия (только быстрый режим)
aiohttp>=3.8.0
aiofiles>=23.0.0
Pillow>=10.0.0
imagehash>=4.3.0
questionary>=2.0.0
brotli>=1.0.0

# Для сборки .exe (опционально)
pyinstaller>=6.0.0